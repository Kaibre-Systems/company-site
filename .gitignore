node_modules/
.next/
.swc/
.DS_Store

# Environment — never commit real values. `.env.example` is intentionally tracked.
.env
.env.*
!.env.example

# Build / tooling artifacts
*.tsbuildinfo

# Logs
logs/
*.log

# Local QA output. The scripts under qa-mobile/ are tracked; the runs they
# write beside themselves are not — they are hundreds of screenshots per sweep.
qa-screenshots/
qa-mobile/*/
playwright-report/
test-results/
